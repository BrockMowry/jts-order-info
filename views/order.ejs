<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous" />
</head>

<body>
    <div class="container p-5">
        <header>
            <p class="text-center">
                <img src="../imgs/outdoor-logo.webp" alt="jts-logo" class="img-fluid" />
            </p>
        </header>

        <h1 class="display-6 my-4">
            <%= order.orderNumber %>
        </h1>

        

        <div class="row gap-5">
            <div class="col">
                <h5>Billing Address</h5>
                <p class="lead">
                    <%= order.billingAddress.name.trim() %><br />
                    <% if (order.billingAddress.company != undefined) { %>
                        <%= order.billingAddress.company.trim() %><br />
                    <% } %>
                    <%= order.billingAddress.address1.trim() %><br />
                    <% if (order.billingAddress.address2 != undefined) { %>
                        <%= order.billingAddress.address2.trim() %><br />
                    <% } %>
                    <%= order.billingAddress.city.trim() %>, <%= order.billingAddress.province.trim() %> <%= order.billingAddress.postalCode.trim() %>
                </p>
            </div>
            <div class="col">
                <h5>Shipping Address</h5>
                <p class="lead">
                    <%= order.shippingAddress.name.trim() %><br />
                    <% if (order.shippingAddress.company != undefined) { %>
                        <%= order.shippingAddress.company.trim() %><br />
                    <% } %>
                    <%= order.shippingAddress.address1.trim() %><br />
                    <% if (order.shippingAddress.address2 !=undefined) { %>
                        <%= order.shippingAddress.address2.trim() %><br />
                    <% } %>
                    <%= order.shippingAddress.city.trim() %>, <%= order.shippingAddress.province.trim() %> <%= order.shippingAddress.postalCode.trim() %>
                </p>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN"
        crossorigin="anonymous"></script>
</body>

</html>